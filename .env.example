APP_PORT=8001
DATABASE_URL=postgresql://user:password@localhost:5433/vulntrack
APP_PORT=8001
POSTGRES_USER=vuln
POSTGRES_PASSWORD=change
POSTGRES_DB=vuln
DATABASE_URL=postgresql://vulntrack:change@localhost:5433/vulntrack
REDIS_URL=redis://localhost:6380/0
API_KEYS=dev-key-changeme-in-production
RATE_LIMIT_DEFAULT=100/minute
RATE_LIMIT_WRITE=20/minute
RATE_LIMIT_AUTH=10/minute
# Generer avec: openssl rand -hex 32
JWT_SECRET_KEY=changeme-generate-with-openssl-rand-hex-32
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=30
CORS_ORIGINS=
ENVIRONMENT=development
# Workers uvicorn par instance api (voir README, Repartition de charge et replicas)
UVICORN_WORKERS=2
# Identifiants admin de Grafana (a changer avant toute exposition reelle)
GRAFANA_USER=admin
GRAFANA_PASSWORD=admin
